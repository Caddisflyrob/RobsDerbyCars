@model IEnumerable<RobsDerbyCars.Models.Heat>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstRacer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SecondRacer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstRacerIsWinner)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsComplete)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FirstRacer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SecondRacer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FirstRacerIsWinner)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsComplete)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HeatID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HeatID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HeatID })
        </td>
    </tr>
}

</table>
